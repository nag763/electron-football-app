const assert = require('assert');

import {generateHTMLtd, generateHTMLtr} from '../js/utils/htmlutils.js';

describe('Test for HTML TD generator', function(){
  it('should be true', function() {
    assert.equal(true, true);
  })
})
